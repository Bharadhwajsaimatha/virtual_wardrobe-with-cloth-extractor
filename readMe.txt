We have packaged everything and built executable files.

To extract the tshirt foreground from any image with a single tshirt it in it, use:
Tee_extractor.exe application

For the virtual try-on, use
virtual_wardrobe.exe application